name: gitlab_15.0.3-ce.0
on:
  push:
    branches:
      - 'main'
    paths:
      - '.github/workflows/gitlab_15.0.3-ce.0.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Docker BuildX
        uses: docker/setup-buildx-action@v2

      - name: Login in AliYun Registry
        uses: docker/login-action@v1
        with:
          registry: registry-intl.cn-hangzhou.aliyuncs.com
          username: ${{ secrets.ALIYUNHUB_USERNAME }}
          password: ${{ secrets.ALIYUNHUB_TOKEN }}

      - name: Build and push
        run: |
          docker pull gitlab/gitlab-ce:15.0.3-ce.0
          docker tag gitlab/gitlab-ce:15.0.3-ce.0 registry-intl.cn-hangzhou.aliyuncs.com/fky357/gitlab-ce:15.0.3-ce.0
          docker push registry-intl.cn-hangzhou.aliyuncs.com/fky357/gitlab-ce:15.0.3-ce.0
